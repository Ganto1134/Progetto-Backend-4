@model IEnumerable<Polizia.Models.TipoViolazione>

@{
    ViewData["Title"] = "Tipi di Violazioni";
}

<h1>Tipi di Violazioni</h1>

<p>
    <a class="btn btn-primary" asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Descrizione)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Descrizione)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.IdViolazione">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.IdViolazione">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.IdViolazione">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
